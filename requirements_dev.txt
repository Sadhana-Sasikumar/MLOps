pymongo
pymongo[src]
dnspython
pandas
numpy
ensure
pytest

-e .

# dev requirements -

pytest==7.1.3
tox==3.25.1 #for testing our code in diff diff env
black==22.8.0 #for linting tool - indentation in a for loop not following it then it gives the warning when not following the protocol
flake8==5.0.4  # for linting
mypy==0.971 #for linting
